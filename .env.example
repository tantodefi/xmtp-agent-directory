# Agent Directory Environment Variables

# ===========================================
# Development Mode
# ===========================================

# When true, loads agents from example-agents.json (dev/testing)
# When false, loads agents from individual JSON files in app/data/ (production)
DEV_MODE=true

# ===========================================
# XMTP Configuration (for uptime checker cron)
# ===========================================

# Private key for the backend pinger wallet (0x prefixed hex)
# This wallet is used to send "ping" messages to agents to check their liveness
# Generate with: openssl rand -hex 32 | sed 's/^/0x/'
XMTP_WALLET_KEY=0x...

# Encryption key for XMTP local database (0x prefixed hex, 32 bytes)
# Required for XMTP client persistence
# Generate with: openssl rand -hex 32 | sed 's/^/0x/'
XMTP_DB_ENCRYPTION_KEY=0x...

# XMTP network environment
# 'dev' for testing/development, 'production' for mainnet
XMTP_ENV=dev

# ===========================================
# Vercel Cron Authentication
# ===========================================

# Secret token to authenticate cron job requests
# Generate with: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here
NEXT_PUBLIC_URL=